version: "3.9"

services:
  mqtt-addon:
    image: python:3.12-slim
    working_dir: /app
    command: ["bash", "-lc", "pip install fastapi uvicorn && uvicorn app.main:app --host 0.0.0.0 --port 8080"]
    volumes:
      - ../:/app
    ports:
      - "18080:8080"
