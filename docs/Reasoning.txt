Task #13.1 :
Error context: None.
Reasoning: Implementing bootstrap v2 safely requires explicit baseline documentation of the current script behavior so each follow-up change can be validated against scope constraints.
What and why: Added Ducuments/bootstrap_install.md documenting current bootstrap prompts/version resolution/install layout/compose startup, plus the v2 target flow and concrete breaking behavior changes required by Task 13.
###########
Task #13.2 :
Error context: Initial resolver implementation mixed log output with parsed stdout and produced invalid release URL parsing; fixed by routing resolver logs to stderr.
Reasoning: Release resolution must be robust and deterministic across API failures/rate limits, while keeping script output machine-parseable for downstream steps.
What and why: Added `--version <tag|latest>`, GitHub API release resolution with HTML fallback for latest, explicit tag resolution via releases/tags API, and checksum handling that verifies against release checksum files when present or prints computed SHA256 otherwise; updated script documentation with new behavior.
###########
Task #13.3 :
Error context: None.
Reasoning: Bootstrap reruns should be safe and fast, avoiding redundant download/extract work while still allowing explicit refresh behavior.
What and why: Added deterministic idempotent install behavior that skips download/extract when the selected version is already installed, reports when `current` already points to the requested version, and adds `--force` to re-download/re-extract; updated script docs with idempotency and force usage.
###########
Task #13.4 :
Error context: None.
Reasoning: Task 13 requires bootstrap startup to be explicit addon-only behavior regardless of compose contents, while still letting operators select host bind/port settings.
What and why: Added `--addon-port` and `--bind` CLI options with validation, generated a compose port override file for mqtt-addon binding, and changed startup to `docker compose ... up --no-deps mqtt-addon` to prevent broker container startup; updated scripts documentation with addon-only and bind/port usage.
###########
Task #13.5 :
Error context: None.
Reasoning: Bootstrap completion should reflect actual service readiness, and browser launch behavior must be configurable for headless automation and operator convenience.
What and why: Added health polling with timeout and progressive status logs, plus UI open behavior using `xdg-open`/`open` with fallback URL printing; added `--no-open` and `--timeout-seconds` CLI controls and documented readiness/open options.
###########
Task #13.6 :
Error context: Per operator request, deferred running bootstrap validation execution until after Task 14.
Reasoning: Final step should provide a reproducible validation entrypoint and quick-start guidance while allowing validation execution to be coordinated later.
What and why: Added scripts/validate-bootstrap.sh to run bootstrap with default validation args, check /healthz, and print UI URL; updated README Quick Start and scripts documentation to include bootstrap v2 usage and validation helper.
###########
Task #14.1 :
Error context: None.
Reasoning: Wizard flow requires stable backend truth for setup progress independent of transient health snapshots, so install status must include persisted state fields.
What and why: Added persisted install session state (`runtime/install_state.json`) with `mode/configured/verified/registered_to_core/last_error`, surfaced these fields in `/api/install/status`, and updated install apply error/success paths to keep state synchronized; documented model/state in Ducuments/models.md.
###########
Task #14.2 :
Error context: None.
Reasoning: External setup UX needs machine-readable diagnostics for connection failures and consistent install-session state transitions during test/apply actions.
What and why: Extended `/api/install/test-external` response with `diagnostic_code` and mapped reason strings into stable codes; updated test/apply handlers to synchronize `mode/configured/verified/last_error` state in `runtime/install_state.json`; documented API and model updates.
###########
Task #14.3 :
Error context: None.
Reasoning: Embedded mode requires a concrete runtime enablement mechanism, not just persisted settings, plus restart flows must feed wizard verification status.
What and why: Added embedded runtime application that writes `runtime/broker` files, generates compose override, and attempts compose up for `mosquitto` + `mqtt-addon`; enabled `/api/install/apply` embedded branch with operator-action fallback; wired broker restart endpoint to update install-session verification state; updated API/services docs.
###########
Task #14.4 :
Error context: None.
Reasoning: Core registration needs compatibility across route variants and explicit install-session registration tracking so wizard completion state remains accurate.
What and why: Updated Core registry service to prefer `POST /api/addons/registry/{addon_id}/register` with fallback to legacy `/api/admin/addons/registry`; updated `/api/install/register-core` to persist `registered_to_core`/`last_error` and emit clearer auth/unreachable errors; updated API/services docs.
###########
Task #14.5 :
Error/UX issue: Setup reset action required by task spec was missing a backend endpoint, so UI could not reliably clear server-side install state.
Reasoning: Wizard v1 should guide both embedded and external setup end-to-end with durable non-secret browser state, clear status visibility, and explicit reset behavior.
What and why: Implemented six-step setup wizard UI under `/ui` with mode-specific forms, validation, test/apply/register actions, status banner, done-page copy helpers, and localStorage persistence without raw secrets; added `POST /api/install/reset` plus config-store reset helper so reset clears backend install-session state and returns to external mode defaults.
###########
Task #14.6 :
Error/UX issue: `/ui` always opened setup wizard, so configured deployments lost the normal post-setup dashboard path.
Reasoning: Setup gating should be deterministic from backend install state so first-run operators land in setup while configured systems default to runtime dashboard workflows.
What and why: Added `/ui` client-side gating based on `GET /api/install/status` (`configured` flag), splitting UI into setup and dashboard views; setup remains available, and configured state now defaults to dashboard with status refresh, effective-config summary, and test publish actions.
###########
