Task #060 :

Reasoning: Existing telemetry was Core-facing and not operator-facing; dashboard lacked local MQTT usage trend visibility.

What and why:
Added runtime MQTT usage metrics endpoint (`GET /api/mqtt/metrics`) with publish/deny/reconnect counts, active registration count, per-addon usage summaries, and broker mode summary. Added dashboard Usage Metrics panel wired to the new endpoint.

###########
