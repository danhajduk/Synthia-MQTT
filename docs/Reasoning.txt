Task #49 :
Reasoning: Direct MQTT registrations need stable long-term credentials without storing plaintext secrets in persisted records; reprovision must rotate credentials deterministically.
What and why:
- Extended MQTT registration request with `reprovision` flag.
- Added direct credential response model (`username`, `password`) for direct access modes.
- Implemented deterministic credential derivation from persisted local seed + version counter and stored only credential metadata (`username`, `version`, `password_hash`).
- Implemented reprovision rotation by incrementing credential version.
- Updated docs to describe direct credential provisioning and reprovision behavior.
###########
