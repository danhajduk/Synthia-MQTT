Task #46.2 :
Reasoning: Operators need an explicit UI control for the backend `allow_unvalidated` override path so default behavior remains safe while still allowing deliberate bypass.
What and why:
- Added setup apply-step checkbox to allow unvalidated external apply (with warning text).
- Persisted checkbox state in local setup state and sent `allow_unvalidated` in external apply payload.
- Hid override control for embedded mode to avoid irrelevant UI noise.
- Rebuilt frontend distribution assets.
###########
