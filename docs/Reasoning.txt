Task #54 :
Reasoning: HA discovery/state publishing must be explicitly governed per registration so access is auditable and enforceable by mode.
What and why:
- Added HA mode grant to registration contract/records: `none`, `gateway_managed`, `direct_allowed`.
- Updated HA discovery sensor endpoint to require `addon_id` and enforce registration `ha_mode != none`.
- Added HA gateway state helper endpoint: `POST /api/ha/discovery/state/publish` with same HA mode enforcement.
- Wired telemetry tracking for HA state publish and updated API/README docs.
###########
