Task #45.1 :
Reasoning: Embedded broker management requires a stable container identity and observable runtime status in install health snapshots.
What and why:
- Standardized embedded broker container name to `synthia-addon-mqtt-mosquitto` for compose override generation and broker management operations.
- Added runtime broker inspection (`docker socket available` and `broker running`) to `GET /api/install/status`.
- Reloaded MQTT client after successful embedded apply so the addon reconnects using embedded-mode runtime configuration.
- Updated deployment/API docs for the stable container name and new status fields.
###########
