Task #44.1 :
Reasoning: Persist broker mode selection independently from full apply so setup state can move from `unconfigured` to `configuring` as soon as the operator chooses a mode.
What and why:
- Added `POST /api/install/mode` to persist selected mode (`external|embedded`) before apply.
- Added mode-update request/response models and ConfigStore mode persistence helper.
- Updated setup-state handling so an unconfigured install reports `configuring` after mode selection save.
- Updated endpoint docs in `README.md` and `docs/api.md`.
###########
