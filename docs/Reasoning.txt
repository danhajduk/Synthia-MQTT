Task #16.1 :
Error context: Release artifact script excluded frontend by default, causing `/ui` assets to be missing in extracted installs.
Reasoning: Release packaging must always include frontend files and guarantee `frontend/dist` exists before tarball creation.
What and why: Updated `scripts/release-addon.sh` default package paths to include `frontend` and added an automatic `npm run build` fallback when `frontend/dist` is missing; documented release script behavior in scripts docs.
###########
Task #16.2 :
Error context: Previous release package lacked UI assets and version metadata was behind required release number.
Reasoning: Releasing `0.1.6` with regenerated signed artifacts ensures installers pull a package that includes frontend UI files.
What and why: Bumped manifest version to `0.1.6`, rebuilt/signed artifacts via `scripts/sign-addon.sh`, confirmed `frontend/dist` entries inside package tarball, and published `v0.1.6` with refreshed `release-output.json` metadata.
###########
#############
All Tasks completed 03/05/2026 14:06:00
#############
Task #17.1 :
Error/UX issue: Embedded apply operator command was relative-path only, and docker-missing warnings were too raw for operators.
Reasoning: Operator-action output should be copy/paste safe from any directory and error guidance should clearly direct users to host-terminal execution.
What and why: Updated install apply failure response to return `cd <addon-root> && docker compose ...` operator action, and normalize docker-missing warning text to an explicit host-terminal instruction; documented API response behavior.
###########
