Task #30 :
Error context: New requested install layout requires service symlink path under `./SynthiaAddons/service/<addon_id>` while preserving artifact-first install flow.
Reasoning: Keep existing install behaviors that already satisfy folder/version/artifact requirements and adjust only the symlink path + docs to match the new layout request.
What and why: Updated bootstrap symlink root to `./SynthiaAddons/service`, and synced README/bootstrap/scripts docs to the same path.
###########
