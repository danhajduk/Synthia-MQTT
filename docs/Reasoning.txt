Task #057 :

Reasoning: Operators needed a single place to inspect active MQTT registrations and setup capabilities without reading runtime files.

What and why:
Added GET /api/mqtt/registrations to return setup capability summary plus registration records (access mode, scopes, HA mode, broker profile, direct username, health signal) and added a dashboard registration inspector panel wired to this endpoint.

###########
