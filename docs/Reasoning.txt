Task #061 :

Reasoning: External broker mode needed explicit direct-access governance rules so operators are not misled into assuming managed broker-side provisioning exists.

What and why:
Added explicit external direct-access model (`gateway_only` vs `manual_direct_access`) to install mode/apply/status APIs and setup UI. Registration now enforces mode-specific behavior: gateway-only rejects direct registrations; manual-direct mode allows recording manual credential mapping (`manual_direct_mqtt`) without fake broker credential provisioning.

###########
