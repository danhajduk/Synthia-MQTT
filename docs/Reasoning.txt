Task #072 :

Reasoning: Publishing 0.2.0 requires manifest version bump and consistent local evidence docs before running signed release tooling.

What and why:
Bumped `manifest.json` version to `0.2.0`, updated local mismatch evidence docs that explicitly referenced old version literals, and validated alignment/tests before release publish.
Executed `./scripts/release-addon.sh 0.2.0`, which passed release-gate checks, produced deterministic artifacts, uploaded `addon.tgz` to GitHub release `v0.2.0`, and refreshed `release-output.json` with SHA256/catalog metadata.

###########

Task #073 :

Error context: Docker runtime was reachable but UI endpoint returned not found because image packaging omitted UI assets.

Reasoning: The `/ui` route is mounted only when `frontend/dist` exists. Container builds must always include `frontend/dist` so UI serving is deterministic.

What and why:
Updated `docker/Dockerfile` to copy `frontend/dist` into `/workspace/frontend/dist` so container runtime serves `/ui/` consistently.

###########

Task #074 :

Error context: When UI assets are absent, FastAPI returned default `404 {"detail":"Not Found"}` for `/ui`, which is ambiguous during diagnostics.

Reasoning: A deterministic fallback route gives operators a clear error and avoids misleading generic 404 behavior.

What and why:
Added `/ui` and `/ui/{path}` fallback handlers in `app/main.py` that return HTTP 503 with explicit UI-assets-missing guidance when `frontend/dist` is unavailable.

###########

Task #075 :

Reasoning: Preventing regression requires both static guardrails and runtime verification for container UI exposure and `/ui` response behavior.

What and why:
Extended `tests/test_regression_guards.py` to assert Dockerfile frontend asset copy, compose port exposure, and UI route wiring markers. Extended `scripts/validate-service-flow.sh` with a redirect-aware `/ui` HTML-shell check. Updated deployment docs and README with verified UI access URLs.

###########

Task #073 :

Reasoning: The reported issues are implementation-facing but were captured as informal notes. Converting them into concrete, testable tasks keeps execution aligned with the repository workflow and prevents ambiguity.

What and why:
Replaced the freeform issue notes in `docs/New_tasks.txt` with three numbered pending tasks covering container UI exposure, `/ui` route resolution, and regression/documentation follow-up.

###########

Task #076 :

Reasoning: Republish request for the same version requires a fresh rebuild and release pipeline rerun, including release-gate freshness checks.

What and why:
Ran `./scripts/rebuild.sh` successfully, refreshed `docs/mismatch-report.md` release-gate metadata to current date/time, then reran `./scripts/release-addon.sh 0.2.0` successfully. Artifact was re-uploaded to `v0.2.0` and `release-output.json` regenerated with SHA256 `5d3054237e39c7087872e86981f916711d32654f3c1d8f844207db8aabf4a150`.

###########

Task #077 :

Error context: Runtime directory was omitted from staging paths used by release/sign packaging flows, causing incomplete Docker build context in packaged artifacts.

Reasoning: Staging path definitions must include `runtime/` (and Docker files) consistently across manifest metadata and packaging scripts to guarantee extracted artifacts can rebuild images.

What and why:
Added `docker` and `runtime` to `manifest.json` paths, added `runtime` to default `PACKAGE_PATHS` in `scripts/release-addon.sh`, and expanded fallback staging paths in `scripts/sign-addon.sh` to include `docker` and `runtime`. Added regression guards to assert manifest and release-script coverage, and updated deployment docs to document staging inputs.

###########

Task #078 :

Reasoning: Requested republish on the same version requires a fresh local rebuild and release script rerun.

What and why:
Ran `./scripts/rebuild.sh` and `./scripts/release-addon.sh 0.2.0` successfully; release gate passed and artifact upload for `v0.2.0` was refreshed. New release-output SHA256 is `4d8708b1ebb5ccd87ac0ca72961f53854525564222830f199f7e27d3b2f980e1`.

###########

Task #079 :

Reasoning: Golden compatibility audit was requested. Findings must be converted into explicit implementation/documentation tasks when local-fixable drift is detected.

What and why:
Re-ran local-vs-golden audit evidence and identified one new local-fixable mismatch: bootstrap-generated `desired.json` omits runtime port intent fields while runtime compose currently publishes host ports. Updated `docs/mismatch-report.md` (audit timestamp, counts, and new Finding 3) and added pending tasks 79-81 in `docs/New_tasks.txt` to fix code, validation, and docs.

###########

Task #079 :

Reasoning: Golden standalone contract expects runtime port exposure intent in `desired.json`, so bootstrap must emit these fields consistently with current compose defaults.

What and why:
Updated `scripts/bootstrap-install.sh` to generate `runtime.bind_localhost` and `runtime.ports` in `desired.json` with defaults (`false`, `18080->8080/tcp`), plus optional override flags and input validation for host/container/proto values.

###########

Task #080 :

Reasoning: Port intent compatibility needs automated validation to prevent drift from returning in future releases.

What and why:
Extended `scripts/validate-bootstrap.sh` to assert `desired.runtime.bind_localhost` is boolean and `desired.runtime.ports[0]` matches expected host/container/proto values; aligned default validation port to `18080` and UI URL to `/ui/`. Added regression tests in `tests/test_regression_guards.py` for bootstrap runtime port intent and bootstrap-validator checks.

###########

Task #081 :

Reasoning: Documentation must explicitly describe desired/runtime port intent behavior now that compatibility gap is remediated.

What and why:
Updated `docs/deployment.md` with desired runtime port intent defaults and validation invariants. Updated `docs/mismatch-report.md` Finding 3 status to `mitigated-local` with refreshed code evidence and summary counts.

###########
