Task #43.2 :
Reasoning: The setup UI must make first-run readiness explicit and prevent users from landing in operational dashboard mode until setup has completed.
What and why:
- Updated setup and dashboard status cards to show `setup_state`, `setup_guidance`, and `direct_mqtt_supported` from `/api/install/status`.
- Changed initialization gate to route users to setup until state is `ready` or `degraded`.
- Rebuilt `frontend/dist` assets from updated source files.
###########
