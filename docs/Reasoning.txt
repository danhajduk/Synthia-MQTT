Task #55 :
Reasoning: Topic governance needs a centralized validation layer with explicit, consistent errors so bad topic contracts are rejected early across direct, gateway, and HA publish paths.
What and why:
- Extended topic-permission module with publish-topic validation helpers.
- Enforced validation in direct publish, gateway publish, and HA gateway state publish APIs.
- Added lifecycle pattern checks for `/announce` and `/health` under addon namespaces.
- Validation now rejects wildcard publish topics, reserved namespace publish attempts, and non-owned addon namespace topics with HTTP 400 reasons.
- Updated docs to describe validation behavior and failure semantics.
###########
