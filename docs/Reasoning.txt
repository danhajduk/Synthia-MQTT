Task #47 :
Reasoning: Health reporting must separate addon runtime status from broker-specific readiness so Core/Supervisor can determine setup completeness and broker availability precisely.
What and why:
- Extended `/api/addon/health` response model with setup and broker fields (`setup_state`, `broker_mode`, `broker_reachable`, `broker_health`, `direct_mqtt_supported`).
- Composed addon health from install session state + MQTT runtime state + embedded broker runtime inspection.
- Added explicit `error`/`unconfigured` status handling for clean readiness signaling.
- Updated API docs with new health fields.
###########
