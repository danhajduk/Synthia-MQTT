Task #40.2 :
Error context: Bootstrap flow stored artifact and compose file but did not materialize an `extracted/` build-context directory expected by SSAP layout guidance.
Reasoning: Extend bootstrap to unpack `addon.tgz` into a deterministic per-version `extracted/` path while preserving existing artifact retention and compose extraction behavior.
What and why: Added artifact extraction step in `scripts/bootstrap-install.sh` to populate `SynthiaAddons/services/mqtt/versions/<version>/extracted`, updated script output summary/comments, and synchronized `docs/deployment.md` with the new extracted-layout behavior.
###########
