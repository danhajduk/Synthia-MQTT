Task #51 :
Reasoning: Gateway publishing should be explicit and structured, with envelope defaults and permission checks derived from approved registration scopes.
What and why:
- Added `POST /api/mqtt/gateway/publish` for structured gateway payload publishing.
- Added standard envelope generation (`type`, `source_addon_id`, `timestamp`, `data`).
- Added default topic/qos/retain behavior and registration-scope topic authorization checks.
- Updated endpoint/model docs and telemetry operation list.
###########
