Task #21 :
Error context: Bootstrap install flow did not create SSAP services-path linkage or desired state file, causing post-install control-plane integration gaps.
Reasoning: Keep the current install root while adding compatibility outputs (`services/<addon_id>` link and `desired.json`) directly in bootstrap so every install produces the expected SSAP structure.
What and why: Updated `scripts/bootstrap-install.sh` to resolve addon id from manifest, create `./SynthiaAddons/services/<addon_id>` symlink to addon root, and write root `desired.json` with version/source metadata; updated bootstrap/script docs to reflect new outputs.
###########
#############
All Tasks completed 03/05/2026 16:55:04
#############
