Task #064 :

Reasoning: Health service incorrectly instantiated the external `AddonHealth` contract model internally, creating schema-coupling risk and runtime validation failures.

What and why:
Introduced internal `HealthSnapshot` dataclass in `app/services/health.py` and switched `HealthService.snapshot()` to return it. API layers continue mapping that snapshot to `AddonHealth` response contracts.

###########
