Task #065 :

Reasoning: Two recent regressions (manifest packaging omission and health snapshot contract coupling) needed automated local guards to prevent reintroduction.

What and why:
Added `tests/test_regression_guards.py` with checks for Dockerfile manifest copy and health snapshot shape, and wired these tests into `scripts/validate-service-flow.sh` so local validation catches these regressions early.

###########
