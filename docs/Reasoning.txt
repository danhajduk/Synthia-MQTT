Task #35 :
Error context: Downloaded artifact extraction showed docker-compose.yml only under docker/ while operator expected it at artifact root.
Reasoning: Mirror docker/docker-compose.yml to root during packaging, matching the existing root Dockerfile mapping pattern.
What and why: Updated release and signing packaging stages to copy docker/docker-compose.yml to root docker-compose.yml when present, and documented root compose in artifact extraction layout.
###########
#############
All Tasks completed 03/05/2026 21:19:40
#############
Task #36 :
Error context: Repository documentation was split between legacy `Ducuments/` files and README statements that no longer matched current code/scripts.
Reasoning: Build a new source-of-truth doc set directly from verified code paths, then archive legacy docs and publish a code-to-doc mismatch report against `/home/dan/Projects/Synthia/docs`.
What and why: Added active docs under `docs/` (`core.md`, `api.md`, `deployment.md`, `mismatch-report.md`), archived all legacy `Ducuments/*.md` into `docs/archive/` with archive headers, and aligned `README.md` plus task tracker to the new documentation baseline.
###########
Task #37 :
Error context: Cross-repo docs contained stale examples and missing optional endpoint references relative to current addon behavior.
Reasoning: Resolve each mismatch directly in the owning Synthia docs and then re-audit the mismatch report from this addon repo.
What and why: Updated `/home/dan/Projects/Synthia/docs/{addons.md,standalone-addon.md,api.md}` and refreshed `docs/mismatch-report.md` to reflect resolved findings.
###########
Task #37 correction:
Error context: Cross-repo golden documentation was edited by mistake; user clarified `/home/dan/Projects/Synthia/docs` is read-only source-of-truth for this repo.
Reasoning: Revert external repo edits and keep alignment work local (tasks + code changes in Synthia-MQTT only).
What and why: Reverted `/home/dan/Projects/Synthia/docs/{addons.md,api.md,standalone-addon.md}` and restored local mismatch report/task tracking to align local implementation against golden docs.
###########
Task #38.1 :
Error context: Privileged addon operations accepted unauthenticated requests, while golden standalone documentation requires local JWT validation for service-token based access control.
Reasoning: Add explicit, scope-based service-token validation behind a configurable auth gate so production can enforce JWT claims without breaking existing local/dev workflows by default.
What and why: Added `ServiceTokenValidator` with HS256 JWT validation (`sub`, `aud`, `jti`, `exp`, `scp/scopes`) and wired scope checks into privileged POST routes (`/api/addon/config`, `/api/mqtt/publish`, `/api/ha/discovery/sensor`, `/api/broker/restart`, `/api/install/apply`, `/api/install/register-core`, `/api/install/reset`). Updated local API/README docs with auth env vars and scope mapping.
###########
Task #38.2 :
Error context: Golden docs require retained policy grants/revocations and local enforcement for service operations, but addon runtime had no policy-topic ingestion or policy gate checks.
Reasoning: Add a local in-memory policy cache fed by retained MQTT policy topics and enforce it specifically on MQTT publish/discovery hot-path operations.
What and why: Added `PolicyCache` service, wired MQTT subscriptions for `<base>/policy/grants/+` and `<base>/policy/revocations/+`, ingested retained payloads into local grant/revocation state, and enforced grant checks in `/api/mqtt/publish` and `/api/ha/discovery/sensor` when `SYNTHIA_POLICY_ENFORCEMENT=true`.
###########
Task #38.3 :
Error context: Addon had no usage reporting path to Core and no offline-safe telemetry buffering despite golden docs requiring periodic usage telemetry with graceful degradation when Core is unavailable.
Reasoning: Implement a local buffered reporter with background flush and persistent queue, then emit usage events from successful publish/discovery operations.
What and why: Added `TelemetryReporter` with queue persistence (`runtime/telemetry_queue.jsonl`), best-effort POST to `/api/telemetry/usage`, retry-on-failure buffering, and startup/shutdown lifecycle hooks. Wired telemetry emission into MQTT publish and HA discovery API success paths.
###########
Task #38.4 :
Error context: Capability identifiers and validation coverage were not fully aligned with documented naming convention and announce/API parity requirements.
Reasoning: Normalize capability IDs to dot-separated tokens and enforce consistency through validation tooling to prevent drift.
What and why: Updated capability literals to `mqtt.publish`, `mqtt.ha.discovery.publish`, `mqtt.ha.state.publish`; aligned telemetry operation naming; extended `validate-service-flow.sh` to verify `/api/addon/capabilities`, compare announce capabilities to API payload, and enforce capability naming regex.
###########
Task #38.5 :
Error context: Bootstrap output still used legacy addon-root layout (`SynthiaAddons/Synthia-MQTT`) instead of canonical SSAP service-root layout (`SynthiaAddons/services/<addon_id>`).
Reasoning: Make canonical service-root the primary install destination, generate both desired/runtime state artifacts there, and keep backward compatibility through a legacy symlink.
What and why: Updated `scripts/bootstrap-install.sh` defaults and output paths to `SynthiaAddons/services/mqtt`, added `runtime.json` generation, and created compatibility symlink `SynthiaAddons/Synthia-MQTT -> SynthiaAddons/services/mqtt`. Synced deployment docs and bootstrap validation helper args with current bootstrap CLI.
###########
Task #38.6 :
Error context: Validation scripts covered baseline health/announce checks but lacked explicit contract checks for auth behavior, policy-topic enforcement, telemetry wiring, and canonical SSAP layout invariants.
Reasoning: Extend existing validation scripts with both runtime assertions and static contract markers to catch drift across security/policy/telemetry/layout requirements.
What and why: Updated `validate-service-flow.sh` with optional auth+policy behavior checks (`AUTH_REQUIRED`, `POLICY_ENFORCEMENT`), token generation for scope validation, policy grant/revocation flow assertions, and static wiring checks for auth/policy/telemetry modules. Updated `validate-bootstrap.sh` to assert canonical `SynthiaAddons/services/mqtt` layout, state files, version artifacts, and legacy compatibility link target.
###########
