Task #48 :
Reasoning: Core-approved MQTT access must be persisted in addon-side registration records with explicit access-mode semantics to support governed gateway/direct usage.
What and why:
- Added `POST /api/mqtt/registrations` endpoint with contract validation.
- Added runtime-backed registration store (`runtime/mqtt_registrations.json`) with upsert behavior.
- Added registration request/record/response models with access modes (`gateway_only`, `direct_mqtt`, `both`).
- Updated README and API docs endpoint inventories.
###########
