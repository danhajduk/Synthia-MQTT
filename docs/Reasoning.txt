Task #56 :
Reasoning: Platform-visible topics need strict envelope validation to prevent malformed event/control payloads from entering MQTT flows.
What and why:
- Added centralized platform-envelope validator for platform topics.
- Enforced schema checks on direct publish and gateway publish paths before broker publish.
- Required envelope fields: `type`, `source_addon_id`, `timestamp`, `data`.
- Enforced message type vocabulary: `announce|health|event|state|command|telemetry|policy`.
- Updated docs for platform envelope validation behavior and HTTP 400 responses.
###########
