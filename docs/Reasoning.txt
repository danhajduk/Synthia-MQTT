Task #40.4 :
Error context: Validation coverage did not enforce the new extracted build-context layout or verify manifest-sourced version wiring in runtime/app metadata.
Reasoning: Add deterministic checks to existing validation scripts so layout/version regressions fail fast in local validation workflows.
What and why: Extended `scripts/validate-bootstrap.sh` to require `versions/<version>/extracted/Dockerfile` and enforce `desired.json.pinned_version` + `runtime.json.active_version` against the `current` symlink version. Extended `scripts/validate-service-flow.sh` static checks to require `version=ADDON_VERSION.version` in `app/main.py`. Updated deployment docs validation bullet list accordingly.
###########
