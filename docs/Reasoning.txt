Task #058 :

Reasoning: Publish failures and denied contracts were not observable from the dashboard, which made broker and policy troubleshooting blind.

What and why:
Added persistent runtime trace logging for publish/discovery/provisioning paths with operation outcome, topic, caller, detail, and optional message/correlation IDs; exposed GET /api/mqtt/publish-traces and added dashboard trace-debugger view with denied/error counters.

###########
