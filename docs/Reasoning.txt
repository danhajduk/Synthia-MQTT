Task #063 :

Reasoning: Service container entered restart loop because addon manifest metadata file was not packaged in the runtime image.

What and why:
Updated `docker/Dockerfile` to copy `manifest.json` into `/workspace/manifest.json` and documented the packaging requirement in deployment docs. Verified image rebuild and container startup status are healthy.

###########
